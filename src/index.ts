import { Poller, PollerInstance } from './poller';
import {
  ResolvePromise,
  CancelTask,
  PollFunction,
  PollerConfig,
} from './types';

export {
  Poller,
  PollerInstance,
  ResolvePromise,
  CancelTask,
  PollFunction,
  PollerConfig as PipeConfig,
};
export default Poller;
